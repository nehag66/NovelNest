/* const multer = require("multer");

// Storage setup (memory for MongoDB)
const storage = multer.memoryStorage(); // Store files as Buffers
const upload = multer({ storage: storage });

module.exports = upload;
 */